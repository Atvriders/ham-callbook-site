# Production-style compose: pulls pre-built images from Gitea container registry.
# For local dev with source rebuilds, use docker-compose.dev.yml.
services:
  backend:
    image: git.waterburp.com/atvriders/ham-callbook-backend:${TAG:-latest}
    environment:
      DB_PATH: /data/USA_Ham_Callbooks.sqlite
      ULS_JSON_PATH: /data/uls.json
    volumes:
      - ./data:/data:ro
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  frontend:
    image: git.waterburp.com/atvriders/ham-callbook-frontend:${TAG:-latest}
    environment:
      NEXT_PUBLIC_API_BASE: /api
    depends_on:
      - backend
    restart: unless-stopped

  caddy:
    image: git.waterburp.com/atvriders/ham-callbook-caddy:${TAG:-latest}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - frontend
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
